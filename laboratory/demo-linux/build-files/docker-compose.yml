version: '3.8'

services:
  vuln-api:
    build: .
    container_name: example-vuln-api
    ports:
      - "5000:5000"
    restart: unless-stopped
    environment:
      - FLASK_ENV=production
    networks:
      - vuln-network

networks:
  vuln-network:
    driver: bridge
